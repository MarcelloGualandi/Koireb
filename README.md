# About `Koireb`

Questo pacchetto è stato creato a lezione dagli studenti.

Il pacchetto si installa cosi:
``r
library(devtools)
install_github(marcellogualandi/koireb)
```

per usare il pacchetto va richiamato con la funzione `library()`
```r
library(koireb)
```
